import styled from 'styled-components'
import { dFlex } from 'styles/uiKit'

const Bar = styled.div`
  ${dFlex.center};
  height: 100%;
  margin-left: auto;
  margin-right: 2px;
`

export default Bar
